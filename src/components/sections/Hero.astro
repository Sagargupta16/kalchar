---
import { getCollection } from 'astro:content';
import { brand, styles } from '@/lib/site';
import { placeholderDataUri } from '@/lib/placeholder';
import { type Artwork } from '@/lib/images';
import ArtworkImage from '@/components/ui/ArtworkImage.astro';
import Chromacard from '@/components/ui/Chromacard.astro';

const all = await getCollection('artworks');
const baseUrl = import.meta.env.BASE_URL;

const featured = (all.find((e) => (e.data as Artwork).featured)?.data ??
  all[0]?.data) as Artwork | undefined;

/* The hero frame's saturated halo + section accent come from the featured
   artwork's first palette swatch. This is what makes "the chrome agrees with
   the art" -- if the featured piece changes, the hero recolors automatically. */
const heroAccent = featured?.palette?.[0] ?? 'var(--color-accent)';
const heroHaloStyle = `--hero-halo:${heroAccent};`;

const placeholderHero = placeholderDataUri({
  title: featured?.title ?? brand.title,
  style: featured?.style ?? 'Madhubani',
  width: 720,
  height: 900,
});

const heroLabel = featured ? `${featured.title} · ${featured.style}` : '';
const heroAlt = featured
  ? `${featured.title}, ${featured.style} painting by ${brand.title}`
  : 'Featured artwork';
---
<section class="relative overflow-hidden border-b border-[var(--color-line)]">
  <div class="aurora" aria-hidden="true"></div>
  <div class="hero-grid" aria-hidden="true"></div>
  <div class="container-x relative z-10 grid gap-10 py-16 sm:gap-12 sm:py-24 md:grid-cols-12 md:items-center md:gap-10 md:py-32">
    <div class="stagger md:col-span-7">
      <p class="t-eyebrow reveal mb-4 sm:mb-5">{brand.tagline}</p>
      <h1 class="t-display reveal text-5xl text-[var(--color-ink)] sm:text-6xl md:text-[7rem] md:leading-[0.95] lg:text-[7.5rem]">
        <span class="block">
          {brand.headline.latinPrefix}<span
            lang="hi"
            class="kinetic-devanagari font-devanagari not-italic"
          >{brand.headline.devanagariCore}</span>
        </span>
        <span class="block text-[0.55em] tracking-[var(--tracking-display)] text-[var(--color-muted)] sm:text-[0.5em]">
          <span class="not-italic">{brand.headline.connector}</span>{' '}
          <span class="text-[var(--color-ink)]">{brand.headline.suffix}</span>
        </span>
      </h1>
      <p class="t-lead reveal mt-5 max-w-xl sm:mt-7">{brand.description}</p>
      <ul class="reveal mt-7 flex flex-wrap gap-x-1.5 gap-y-1.5 sm:mt-10 sm:gap-x-3">
        {styles.map((s) => (
          <li class="t-meta rounded-full border border-[var(--color-line)] px-2.5 py-1 text-[0.65rem] sm:px-3 sm:text-xs">{s}</li>
        ))}
      </ul>
    </div>

    <div class="md:col-span-5">
      <div class="mx-auto w-full max-w-md md:max-w-none" style={heroHaloStyle}>
        <div
          id="hero-frame"
          class="parallax-frame hero-halo relative aspect-[3/4] w-full"
        >
          <div
            data-shadow
            class="absolute inset-0 border border-[var(--color-line)] bg-[var(--color-bg-soft)]"
            style="transform: translate(-1rem, 1rem) translateZ(-30px);"
          ></div>
          <div
            data-inner
            class="relative h-full w-full overflow-hidden border border-[var(--color-line)]"
          >
            {featured?.image ? (
              <ArtworkImage
                filename={featured.image}
                alt={heroAlt}
                baseUrl={baseUrl}
                imgClass="motion-kenburns h-full w-full object-cover"
                pictureClass="block h-full w-full"
                loading="eager"
                fetchpriority="high"
                sizes="(min-width: 768px) 40vw, 90vw"
              />
            ) : (
              <img
                src={placeholderHero}
                alt={heroAlt}
                class="motion-kenburns h-full w-full object-cover"
                loading="eager"
                decoding="async"
                fetchpriority="high"
              />
            )}
          </div>
          {heroLabel && <p class="t-meta absolute -bottom-6 right-0">{heroLabel}</p>}
        </div>
        {featured?.palette && (
          <div class="reveal mt-10 flex items-center gap-3">
            <Chromacard
              palette={featured.palette}
              ariaLabel={`Palette sampled from ${featured.title}`}
            />
            <span class="t-meta whitespace-nowrap">Palette</span>
          </div>
        )}
      </div>
    </div>
  </div>

  <script is:inline>
    (function () {
      if (typeof window === 'undefined') return;
      if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
      if (window.matchMedia('(hover: none)').matches) return;
      const frame = document.getElementById('hero-frame');
      if (!frame) return;
      const shadow = frame.querySelector('[data-shadow]');
      const inner = frame.querySelector('[data-inner]');
      if (!inner || !shadow) return;
      let raf = 0;
      function onMove(e) {
        const r = frame.getBoundingClientRect();
        const x = (e.clientX - r.left) / r.width - 0.5;
        const y = (e.clientY - r.top) / r.height - 0.5;
        cancelAnimationFrame(raf);
        raf = requestAnimationFrame(() => {
          inner.style.transform = `perspective(1000px) rotateX(${(-y * 6).toFixed(2)}deg) rotateY(${(x * 8).toFixed(2)}deg)`;
          shadow.style.transform = `perspective(1000px) translate(${(x * 18 - 16).toFixed(1)}px, ${(y * 18 + 16).toFixed(1)}px) translateZ(-30px)`;
        });
      }
      function reset() {
        inner.style.transform = '';
        shadow.style.transform = 'translate(-1rem, 1rem) translateZ(-30px)';
      }
      frame.addEventListener('mousemove', onMove);
      frame.addEventListener('mouseleave', reset);
    })();
  </script>

  <a href="#work" class="scroll-cue group" aria-label="Scroll to selected work">
    <span class="t-meta block text-center text-[0.6rem]">Scroll</span>
    <span class="scroll-cue__line" aria-hidden="true"></span>
  </a>
</section>
