# UI review and improvements

Reviewed and updated the local site on 14 September 2026, with integration and PR review on 15 September. One dedicated agent audited each of the 23 public, admin, and recovery pages, working in parallel batches. Shared components were integrated and checked together afterward.

The review covered desktop and phone layouts, light and dark themes, keyboard navigation, touch gestures, form drafts, uploads, pending actions, failures, and recovery.

## Shared design and motion

- Consistent 16px corners on cards, fields, buttons, image plates, and panels; 24px corners on sheets. Pills and circular controls keep their shapes.
- Shared spacing for sections, card content, fields, and action groups. Controls maintain a 44px minimum touch target.
- Softer layered shadows, clear surface depth in both themes, and restrained hover and press feedback.
- Removed the long double-line ornaments, repeated heading rules, quote framing, and oversized footer spacing.
- Adapted the portfolio-react motion approach: a shared 400ms ease-out curve, spring interactions, short staggered reveals, and smooth image transitions.
- Animation stays enabled across operating-system motion preferences, as explicitly requested for this pass.
- Floating controls and sheets use a consistent translucent surface. Large surfaces animate opacity and transforms; backdrop blur stays static.
- Mobile navigation locks background scrolling, contains keyboard focus, closes on route selection or Escape, and restores focus and scroll position.
- Theme controls stay synchronized between desktop, mobile, and admin navigation.

## Public pages

| Page or component | Improvements and fixes |
| --- | --- |
| Home | Simpler hero with two clear actions and a full artwork preview. Artwork groups adapt to the number of pieces instead of leaving empty columns. Compact custom-order steps and centered sparse testimonials. |
| Home contact section | Replaced three narrow cards with wide rows showing complete phone, Instagram, and email values. Reduced heading size and empty space. |
| Footer | Compact two-column link groups, smaller brand treatment, and no decorative divider. Measured 304px tall at a 1280px viewport; contact section measured about 420px. |
| Artwork gallery | Flat image grid with uncropped paintings, search by words in any order, independent availability filtering, accurate result counts, URL persistence, and clear empty-result recovery. |
| Artwork viewer | Compact floating toolbar, full-image presentation, thumbnail navigation, horizontal paging and vertical dismissal. Corrected diagonal swipe handling, repeated Escape behavior, modified clicks, and focus restoration. |
| Artwork detail | Appropriate enquiry actions for available, sold, and archive pieces. Long titles and actions wrap. The phone enquiry bar measures its actual height so other controls clear it. |
| About | Improved portrait sizing, wider reading measure, compact introduction, unframed quote, and statistics that wrap as whole units. Loading state includes a useful recovery link. |
| Events | Correct calendar dates, sensible empty-gallery spacing, priority for the first populated gallery, and a stable viewer when an image fails. Touch gestures respect their intended axis. |
| Workshops | Preserved paragraphs and long descriptions, clear WhatsApp actions, a contact alternative, and useful empty-state guidance. |
| Custom orders | Simpler field groups, optional details, focused validation, and clear WhatsApp handoff. The current tab restores the brief and selected options after navigation or reload without resubmitting. |
| Contact | Readable channel rows, wrapped email addresses, distinct social-profile labels, QR guidance, and graceful handling of missing links. |
| FAQ | Clear answers based on the existing enquiry workflow, replacing placeholder copy. Empty questions are omitted from both the page and structured data. Order-specific terms are confirmed directly. |
| Login | Pending feedback prevents repeated sign-in requests. Malformed or repeated callback parameters fall back safely instead of crashing the page. |
| Access denied | Clear distinction between missing access and configuration trouble, with a contact fallback if an administrator address cannot be retrieved. |
| Not found | Verified HTTP 404, noindex, recovery destinations, keyboard focus, and narrow layouts. Existing recovery design retained. |
| Page error | Retry refreshes server data as well as resetting the error boundary. Focus moves to the error heading, and Home performs a fresh navigation. |
| Root error | Self-contained recovery screen that works without the normal layout, fonts, providers, or stylesheet. Retry, reload, and Home remain available. |

## Admin pages

| Page or component | Improvements and fixes |
| --- | --- |
| Navigation | Grouped desktop sidebar and compact mobile tabs with Add and More actions. Unsaved page drafts trigger one confirmation when following admin links. Reload and tab-close warnings cover drafts and unfinished artwork uploads. |
| Pieces | Readable image cards and list rows, clearer filters, accurate counts after edits/deletion, and visible failure recovery. |
| Add piece | Photo-first composer with immediate preview, background staging, real upload progress, validation, and retry. Closing and reopening preserves the draft and upload. Late upload responses cannot replace a newer photo. |
| Edit piece | One Save action for the draft, clear required fields, focus on invalid collapsed fields, preserved replacement-photo selections, and guarded cancellation. Closing sheets stop accepting input. |
| Categories | Clear creation and rename fields, focused errors, retained reorder drafts across refreshes, and retryable deletion feedback. |
| Events | Preserved collapsed drafts, title/date validation, safer pending actions, correct cover and pin feedback, consistent photo validation, keyboard reordering, and protection against losing staged photo order. |
| Workshops | Preserved create/edit drafts and staged order, focused validation, immediate successful creation without duplicate rows, and retryable deletion feedback. |
| Presets | Compact editable rows, retained ordering through refreshes, draft warnings, and clear validation and deletion recovery. |
| Enquiries | Stable selection while filtering and updating, clearer detail and reply actions, visible retry feedback, and correct Undo availability. International phone prefixes are preserved in reply links. Long content and pagination fit smaller screens. |
| Testimonials | Whitespace validation, draft cancellation, immediate creation feedback without duplicates, correct public-placement labels, focus restoration, and protection against competing Undo/delete actions. |
| Profile | Clear photo selection, invalid-file feedback, retained retry drafts, current previews after refresh, and guarded pending actions. |
| Maintainers | Focused validation, clear access labels, preserved drafts, pending feedback, and retryable removal confirmations. |
| Shared feedback | Consistent save, upload, and failure states. Undo cannot be dismissed while its request is pending, so a failure remains visible. |

Affected loading skeletons were updated to match the final page layouts.

## Verification

| Check | Result |
| --- | --- |
| Optimized fixture build | Passed; 43 generated routes. |
| Lint and application/test type checks | Passed. |
| Unit tests | 492 passed across 38 files, including international reply-number and long contact-input regressions. |
| Operational checks | All 18 passed; migration artifacts validated. |
| Production dependency audit | No known vulnerabilities found. |
| Browser regression scenarios | 1,002 passed across the full run and workshop regression rerun. |
| Live admin preview layout checks | 36 passed on port 3010. |
| UI token check | No errors or warnings. |
| Source file size and diff checks | Passed. |
| Manual browser review | Phone and desktop, both themes; gallery/viewer, contact/footer, form persistence, and mobile admin navigation checked. |
| Local code graph | Refreshed after the refactor. |

The final browser sweep covered 1,042 scenarios. The full run passed 1,000, conditionally skipped 40, and found a workshop creation focus race on desktop and phone. Clearing the completed creation state before focusing the new row fixed that race; all 40 workshop checks then passed. The 36 preview-only layout scenarios also passed against the running admin preview. This accounts for 1,038 passing scenarios, with four desktop-only hover/layout cases skipped on the phone project. No browser failures remain unresolved.

## PR review

Three reviewers independently inspected public interactions, admin workflows, and server/data boundaries. Seven actionable findings were corrected before delivery:

- Add destinations now use the same unsaved-draft confirmation as navigation links; cancellation retains the draft and restores focus.
- Global event-photo selections populate the creation form on the current page or after navigation. Photos selected while another event is saving wait for its request to finish.
- A pending artwork action locks other artwork mutations and Undo, preventing dropped requests and misplaced errors.
- Pan limits account for the zoom origin, and existing pan is constrained again when the zoom level changes.
- Changing artwork pages with Back or Forward closes the old detail viewer and releases its scroll lock.
- Artwork and workshop changes invalidate About-page counts.
- Enquiry reply links preserve explicit international phone prefixes.

The artwork-action regressions passed with 76 focused browser checks; the Add workflows passed with 44. Viewer regressions and history checks passed with seven checks against the running preview, and the five new viewer scenarios also passed against the optimized build. These focused counts include existing scenarios rerun around the fixes and are separate from the browser baseline above.

## SonarCloud cleanup

The initial analysis reported 174 unresolved PR findings and 16 older findings on the default branch. The cleanup addressed their source occurrences, including findings that earlier changes had already made stale.

- Keep CSS analysis enabled. Move stylesheet imports to valid positions and express hover shadows, press feedback, and staggered motion as scoped utility selectors. Glass surfaces retain their responsive variants and opaque fallback.
- Use native dialogs, progress elements, status outputs, and fieldsets while preserving focus, keyboard handling, scroll restoration, and accessible descriptions.
- Extract editor, upload, profile, viewer, and filter responsibilities into small components and hooks. Remove redundant string aliases and nested conditions without changing the catalog or authorization boundaries.
- Parse long contact inputs without ambiguous regular-expression backtracking. Preserve international phone prefixes and email reply links.
- Replace deprecated clipboard fallbacks with a selectable email field when copying fails. Keep the retry action and its keyboard focus.
- Replace fixed browser delays and forced interactions with observable UI states and frame-paced touch gestures. Animation remains enabled during verification.
- Release the workshop list's completed creation state before moving keyboard focus to the new row, avoiding a focus attempt on a still-disabled control.

Local SonarJS checks covering 13 of the reported rules pass across the 100 changed source and test files. Application, script, and browser-fixture type checks, lint, UI tokens, all 492 unit tests, and the optimized 43-route build pass. Two additional source reviewers found no new actionable issues in the public, viewer, contact-parsing, or admin changes. Focused browser checks cover the native menu lifecycle, upload progress and descriptions, clipboard failure recovery, profile feedback, and viewer springs.

Fresh remote analysis and CI results are recorded on the PR. Default-branch analysis changes only after the fixes reach that branch.

## Validation boundaries

The local admin on port 3010 uses fixture data and blocks database and storage writes. Browser mutation tests use controlled responses to exercise success, pending, failure, retry, and upload progress. The file-picker check opens the native chooser, selects a bundled artwork, and verifies that the browser decodes its preview.

Production Google authentication, actual database mutations, and real R2 uploads were not exercised. Catalog reads, server authorization, and mutation boundaries remain in place. Local previews use the existing bundled paintings; production image delivery is unchanged.

The local preview is available at http://localhost:3010/. Delivery is tracked in [PR #105](https://github.com/Sagargupta16/kalchar/pull/105), targeting `dev`. Production deployment requires a separate merge to `main`.
